server:
  port: 8010

spring:
  application:
    name: cloud-nacos-payment-service

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: DEV_GROUP
#        命名空间
        namespace: 3a11ffd6-61c5-4887-b8b8-de056a05b204
management:
  endpoints:
    web:
      exposure:
        include: "*"
